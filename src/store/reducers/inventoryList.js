// import {
//   SET_PLACES,
//   REMOVE_PLACE,
//   PLACE_ADDED,
//   START_ADD_PLACE
// } from "../actions/actionTypes";

const initialState = {
    inventoryList: [],
};

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default reducer;
